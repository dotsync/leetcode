const maximumSubarray = arr => {

}
const tester = (actual, expected) => {
  if (actual === expected) console.log('passed');
  else console.log('failed, returns', actual);
};
tester(maximumSubarray());
tester(maximumSubarray());
