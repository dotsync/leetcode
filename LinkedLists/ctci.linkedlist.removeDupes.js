// Remove Dupes (2.1)
const Node = require('./Node');

const firstNode = new Node('I am the first Node');
const secondNode = new Node('I am the second Node');
firstNode.setNextNode(secondNode);

console.log(firstNode);
// console.log(firstNode.getNextNode());

const removeDupes = (head) => {

}

// Time
// Space
