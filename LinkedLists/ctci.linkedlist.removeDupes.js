const Node = require('./Node');
const arrayTester = require('../arrayTester')
// Remove Dupes (2.1)

const removeDupes = (head) => {
  return [1]
}

// Time
// Space

arrayTester(removeDupes([2, 1, 3, 2, 9, 1, 1]), [2, 1, 3, 9]);
