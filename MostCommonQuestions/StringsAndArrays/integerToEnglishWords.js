const integerToEnglishWords = (num) => {
  // Convert a non-negative integer num to its English words representation.
  if (typeof num !== Number) return 'Unable to convert non integers';
};

module.exports = integerToEnglishWords;
